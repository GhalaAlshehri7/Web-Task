CREATE DATABASE IF NOT EXISTS if0_42442058_webtask;

USE if0_42442058_webtask;

CREATE TABLE users (
    id INT(11) AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    age INT(3) NOT NULL,
    status TINYINT(1) NOT NULL DEFAULT 0
);
